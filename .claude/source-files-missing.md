# Danh sách Workbook VBA Thiếu — Ưu tiên Bổ sung (source-files-missing.md)

12 workbook được liệt kê trong yêu cầu rà soát VBA gốc **không có mặt** tại `F:\DF\` tính đến 2026-07-17. Bảng dưới đây xếp hạng ưu tiên bổ sung. **Không được kết luận nhóm Điều phối hoặc nhóm In tem/QR đã rà soát hoàn chỉnh cho tới khi các file mức P0 được cung cấp và audit.**

> [!IMPORTANT]
> **Cập nhật 2026-07-17 (đợt duyệt lần 4) — Mục #4 và #5 chuyển trạng thái `PARTIALLY_RESOLVED`:** Workbook `3.DF028 formulas - PRINTER LANDSCAPE - jit qr sending - 15l special.xlsm` đã được cung cấp và audit đầy đủ (NHÓM 4-DF028, `qr-label-printing-domain.md`). Bằng chứng đối chiếu: (1) tên file trùng khớp gần như hoàn toàn với mục #5 (`DF002 no formulas...15l special.xlsm`, chỉ khác "DF002"↔"DF028"/"no formulas"↔"formulas"); (2) `TO_SEND.frm` có lưới **27 dòng** gửi — khớp "27rows" trong tên mục #4; (3) đầy đủ chức năng gửi QR + ghi `tbl_SentLog` (`ConfirmRow`, `GenerateQRCode`). **Điểm CHƯA xác nhận được:** đã đọc toàn bộ VBA + 100 công thức Excel của DF028, **không tìm thấy nhánh code riêng nào cho "15L"** (xem `b24-warehouse-routing.md` Mục 7) — không thể khẳng định DF028 bao phủ đúng nhánh nghiệp vụ 15L đặc thù của 2 file gốc #4/#5. **Không tự đóng thành `RESOLVED`** — giữ `PARTIALLY_RESOLVED`, ngày xác nhận 2026-07-17, nguồn NHÓM 4-DF028. Xem CH-BUS-011 trong `open-questions.md`.

Ký hiệu mức ưu tiên: **P0** = chặn kết luận "đã rà soát đầy đủ", ảnh hưởng trực tiếp tới độ tin cậy dữ liệu đã di trú hoặc go-live Phase 12. **P1** = cần cho audit đầy đủ nhưng không chặn pilot ngay. **P2** = rủi ro thấp, khả năng cao chỉ là bản sao gần giống bản đã có.

| # | Tên file | Nhóm nghiệp vụ | Lý do cần | Logic nghi ngờ nằm trong file | Bảng Access liên quan | Mức ưu tiên | Rủi ro nếu không có file | Có bản tương đương đã audit không |
|---|---|---|---|---|---|---|---|---|
| 1 | `C3 grid load row lock id FB -(1).xlsm` | Điều phối/Khóa dòng | Là ứng viên số 1 chứa logic ghi/đọc `tbl_ToSend2`/`WAITING`/`tblSync` — 3 bảng có dữ liệu thật trong Postgres nhưng 0 VBA hiện có tham chiếu tới | Cơ chế round-robin đa máy trạm (`NextFE`, `FE1_Alive`..`FE5_Alive`), mapping cột lệch của `tbl_ToSend2`/`WAITING` | `legacy_df_data.tbl_ToSend2` (14 cột, dữ liệu thật), `legacy_df_data.WAITING` (15 cột, lệch cột `ID1`), `legacy_df_data.tblSync` (6 cột heartbeat) | **P0** | Không xác minh được mapping cột trong `03_transform_legacy_to_target.sql` có đúng không; nếu `tblSync` đang vận hành thật ở nhiều máy trạm, Phase 12 (pilot >1 trạm điều phối) có thể mất đồng bộ mà không ai biết | Có 1 phần — `C3 grid load row lock id FB -.xlsm` (bản hiện có) chỉ dùng `tbl_input_all`/`tbl_tosend`/`tbl_sentlog`, KHÔNG chạm 3 bảng trên |
| 2 | `Copy of MACHINE_ID_LOCKED.xlsm` | Điều phối/Khóa dòng | Ứng viên số 2 (cùng lý do với #1) — tên "Copy of" gợi ý đây có thể là bản đã sửa/fork từ `MACHINE_ID_LOCKED.xlsm` | Tương tự #1 | Tương tự #1 | **P0** | Tương tự #1 | Có 1 phần — `MACHINE_ID_LOCKED.xlsm` hiện có cũng chỉ dùng `tbl_tosend`/`tbl_sentlog` |
| 3 | `MACHINE_ID_LOCKED(1).xlsm` | Điều phối/Khóa dòng | Có thể là bản khác `Copy of...` (2 tên khác nhau cho 2 file khác nhau trong yêu cầu gốc — không giả định là trùng nhau) | Tương tự #1 | Tương tự #1 | **P0** | Tương tự #1 | Như trên |
| 4 | `DF002 - PRINTER - qr sending - 15l special-27rows.xlsm` | In tem & QR | Template tem "27 dòng" + khổ "15L special" — hoàn toàn chưa audit được; 2 workbook hiện có chỉ có template "9 rows" | Layout tem 27 dòng, có thể có logic tính riêng cho lô 15L (khổ bồn/mẻ nhỏ) | Không xác định — nghi vấn dùng chung `tbl_tosend`/`tbl_input_all` như 2 file hiện có, nhưng chưa xác minh | **P0** | Nếu nhà máy vẫn dùng lô 15L trong sản xuất thật, hệ thống web hiện tại (chỉ có template tem theo `WeighingJobController`/`PrintJobController`, khổ 80×50mm hoặc động 20-150mm) có thể không đáp ứng đúng layout nghiệp vụ đặc thù này | Không — không có template "27 dòng"/"15L" nào ở cả VBA lẫn web hiện tại |
| 5 | `DF002 no formulas - PRINTER LANDSCAPE - jit qr sending - 15l special.xlsm` | In tem & QR | Cùng nhóm "15L special" + thêm khái niệm "landscape" (in ngang) và "JIT qr sending" (gửi/in tức thời) — cả 2 khái niệm này grep 0 kết quả trong 2 workbook hiện có | Layout in ngang khác chuẩn hiện tại (dọc); cơ chế "JIT" — in ngay khi có dữ liệu thay vì 2 bước gửi/in tách rời như hiện tại (`btnSend_Click` tách biệt `btnPrint_Click` ở bản đang có) | Tương tự #4 | **P0** | Nếu "JIT" là quy trình vận hành thật (in ngay lập tức không qua bước duyệt), kiến trúc `PrintJob::create → poll → in` hiện tại của web có độ trễ khác hẳn — cần xác nhận trước khi coi tốc độ in của web là chấp nhận được | Không |
| 6 | `MACHINE_ID_LOCKED(1).xlsm` *(trùng dòng 3, xem trên)* | — | — | — | — | — | — | — |
| 7 | `张力表-NEW VERSION(1).xlsm` | Công thức & Thông số | Bản `(1)` của workbook chỉ chứa 1 bản sao `TraHeSo` — rủi ro thấp vì bản hiện có (`张力表-NEW VERSION.xlsm`) đã xác nhận gần như trống, chỉ có đúng hàm `TraHeSo` giống hệt bản chính | Khả năng cao chỉ là bản sao lưu, không có logic mới | Không áp dụng | **P2** | Thấp — nếu khác biệt, nhiều khả năng chỉ là version cũ hơn của cùng 1 hàm `TraHeSo` (đã MISSING ở cả 2 bản đang có, xem CH-BUS-004) | Có — bản chính đã audit đầy đủ, nội dung tối giản |
| 8 | `df lượng dùng thuốc nhuộm(1).xlsm` | Công thức & Thông số | Bản `(1)` của công cụ báo cáo/truy vấn (không phải công thức tính toán thật) | Khả năng cao chỉ khác tham số mặc định (`daysBack`) hoặc bản sao lưu | `RECORD.accdb.tblRECORD` (giống bản chính) | **P2** | Thấp — bản chính đã audit đủ 5 procedure, đều thuộc dạng truy vấn báo cáo, rủi ro nghiệp vụ thấp | Có — bản chính đã audit đầy đủ |
| 9 | `SEMI CHECKER(1).xlsm` | Cân bán tự động | Công cụ kiểm tra bán thành phẩm — bản chính (SEMI CHECKER.xlsm = "A" trong nhóm SCALE) đã audit kỹ, không phát hiện dấu hiệu đây là workbook có nhiều biến thể lớn (khác semiauto B/C) | Khả năng cao chỉ khác đường dẫn `putty_log.txt` hoặc bản sao lưu, theo đúng pattern đã thấy giữa A/B/C của nhóm cân | `RECORD.accdb.tblRECORD` | **P1** | Trung bình — SEMI CHECKER dùng để QA/QC bán thành phẩm, nếu (1) có quy tắc PASS/FAIL khác thì ảnh hưởng chất lượng, nhưng chưa có bằng chứng cụ thể nào gợi ý khác biệt lớn | Có — bản chính đã audit đầy đủ |
| 10 | `semiauto-Checker plus-accept_reject semi 9rows lockmove SEND OVER6 - low stand1.xlsm` | Cân bán tự động | Tên file chứa "low stand1" — thuật ngữ **0 kết quả grep** trong cả 3 workbook cân hiện có; cần để làm rõ CH-TECH-005 | Nghi vấn: ngưỡng/luật cân bổ sung khác với ngưỡng ±1% đã xác nhận ở workbook B/C hiện có | `Z:\DF_SCALE\RECORD.accdb.tblRECORD` (cùng họ với A/B/C) | **P1** | Trung bình — nếu "low stand1" là 1 mức dung sai/luật accept-reject bổ sung đang dùng thật, thuật toán cân mới (`WeighingJobController::weighItem`) có thể thiếu 1 nhánh luật quan trọng | Không — "low stand1" chưa xuất hiện ở đâu trong 3 workbook đã audit |
| 11 | `semiauto-chem-deltaRaw 8rows.xlsm` | Cân bán tự động | Tên file có "8rows" — khác "9rows" đã xác nhận ở 2/3 workbook hiện có; cần làm rõ đây có phải luồng cân hóa chất riêng (8 dòng thay vì 9) hay không | Nghi vấn: luồng cân hóa chất tách biệt khỏi luồng cân thuốc nhuộm chính, số dòng khác (8 thay vì 9) | Tương tự | **P1** | Trung bình — nếu có luồng cân hóa chất riêng biệt với số dòng khác, `WeighingJobController` hiện tại (dùng chung 1 mô hình `weighing_job_items` cho cả dye/chemical) có thể chưa phản ánh đúng cấu trúc 8-dòng này | Không xác nhận — "8 rows" chưa audit được |
| 12 | `in tem Copower(1).xlsm` | In tem & QR | Bản `(1)` của workbook A (đã audit, là tập con module của workbook B/QR PRINTER) — rủi ro thấp vì B đã chứa toàn bộ chức năng của A cộng thêm | Khả năng cao là bản sao lưu hoặc phiên bản trung gian giữa A và B | `Z:\DF\DATA\record.accdb` (tbl_tosend, tbl_input_all — giống A/B hiện có) | **P2** | Thấp | Có — cả A và B (bao trùm A) đã audit đầy đủ |
| 13 | `QR PRINTER-send to access- NEW 9ROWS BIG QR(1).xlsm` | In tem & QR | Bản `(1)` của workbook B (đầy đủ nhất trong 2 bản hiện có) | Khả năng cao là bản sao lưu | Tương tự B | **P2** | Thấp | Có — bản B đã audit đầy đủ, là bản "đầy đủ nhất" trong nhóm |

## Tổng hợp theo mức ưu tiên

- **P0 (5 file — bắt buộc trước khi coi Điều phối/In tem là rà soát xong):** `C3 grid load row lock id FB -(1).xlsm`, `Copy of MACHINE_ID_LOCKED.xlsm`, `MACHINE_ID_LOCKED(1).xlsm`, `DF002 - PRINTER - qr sending - 15l special-27rows.xlsm`, `DF002 no formulas - PRINTER LANDSCAPE - jit qr sending - 15l special.xlsm`.
- **P1 (3 file — nên có trước UAT Phase 12 nhóm Cân):** `SEMI CHECKER(1).xlsm`, `semiauto-Checker plus-accept_reject semi 9rows lockmove SEND OVER6 - low stand1.xlsm`, `semiauto-chem-deltaRaw 8rows.xlsm`.
- **P2 (4 file — rủi ro thấp, có thể trì hoãn):** `张力表-NEW VERSION(1).xlsm`, `df lượng dùng thuốc nhuộm(1).xlsm`, `in tem Copower(1).xlsm`, `QR PRINTER-send to access- NEW 9ROWS BIG QR(1).xlsm`.

## Kết luận chính thức về mức độ hoàn chỉnh của rà soát theo nhóm

- **Nhóm Điều phối/Khóa dòng (DISPATCH): CHƯA HOÀN CHỈNH** — thiếu 3 file P0, không thể xác minh `tbl_ToSend2`/`WAITING`/`tblSync`. **Cập nhật 2026-07-17:** audit DF028 xác nhận `tbl_sentlog` có nguồn ghi thật (`ConfirmRow`), thu hẹp 1 phần câu hỏi mở CH-TECH-004 — nhưng nguồn ghi `tbl_input_all` (nghi vấn thuộc C3/MID) vẫn chưa audit sâu, 3 file P0 vẫn cần bổ sung.
- **Nhóm In tem & QR (PRINT): CÓ THỂ ĐÃ ĐÓNG ĐƯỢC 2/5 FILE P0 (cập nhật 2026-07-17)** — workbook DF028 vừa được cung cấp và audit đầy đủ (NHÓM 4-DF028) khớp tên gần như hoàn toàn với mục #4/#5 (xem cảnh báo đầu tài liệu) — chờ người dùng xác nhận đây có phải chính là 2 file đó không. Nếu đúng, PRINT chỉ còn thiếu 2 file P2 (rủi ro thấp). Nếu KHÔNG đúng (DF028 là file thứ 3 cùng họ), vẫn cần bổ sung cả #4/#5 gốc.
- **Nhóm Cân bán tự động (SCALE): CƠ BẢN ĐẦY ĐỦ, còn 3 điểm cần xác nhận (P1)** — thuật toán cốt lõi (StableFilter/delta/tare/OVER6/accept-reject ±1%) đã audit đầy đủ trên 3/6 workbook liên quan; 3 file còn thiếu chủ yếu làm rõ thuật ngữ "low stand1"/"8 rows", rủi ro nghiệp vụ trung bình.
- **Nhóm Công thức (RECIPE): ĐẦY ĐỦ** — 2 file thiếu đều mức P2, rủi ro thấp.
- **Nhóm Xử lý sự cố (TROUBLE): ĐẦY ĐỦ** — không có file nào bị thiếu trong yêu cầu gốc cho nhóm này.
